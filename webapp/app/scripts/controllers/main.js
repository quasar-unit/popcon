'use strict';

/**
 * @ngdoc function
 * @name popconApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the popconApp
 */
angular.module('popconApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
